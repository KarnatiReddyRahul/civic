# CivicAssist AI Environment Configuration Template

# FastAPI backend URL (Streamlit sends all API requests here)
API_BASE=http://localhost:8001

# Ollama / Llama 3 API base URL for generating complaint letters
AI_GENERATOR_BASE=http://localhost:11434

# SMTP configuration for email dispatch (optional — falls back to log)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@civicassist.ai
